// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://wdrav.github.io',
  // If your repo name is NOT <username>.github.io, uncomment and set this:
  // base: '/your-repo-name',
});
